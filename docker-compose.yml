version: '2'
volumes:
  unifi-config:
services:
  backup:
    build: ./backup
    restart: unless-stopped
    volumes:
      - "unifi-config:/config"
  unifi-controller:
    depends_on:
      - backup
    image: linuxserver/unifi-controller:arm32v7-5.11.46-ls35
    ports:
      - "3478:3478/udp"
      - "6789:6789"
      - "8080:8080"
      - "8081:8081"
      - "8443:8443"
      - "8843:8843"
      - "8880:8880"
      - "10001:10001/udp"
    restart: unless-stopped
    volumes:
      - "unifi-config:/config"
